import { OrdenamientoConPipesPipe } from './ordenamiento-con-pipes.pipe';

describe('OrdenamientoConPipesPipe', () => {
  it('create an instance', () => {
    const pipe = new OrdenamientoConPipesPipe();
    expect(pipe).toBeTruthy();
  });
});
